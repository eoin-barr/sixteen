### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String
  user: User!
}

type Message {
  id: Int
  text: String
  userId: Int
}

type Mutation {
  createMessage(text: String!, userId: Int!): Message!
  deleteMessage(messageId: Int!): Message
  login(email: String!, password: String!): AuthPayload
  signup(email: String!, name: String!, password: String!): AuthPayload!
}

type Query {
  message(messageId: Int!): Message!
  messages: [Message]!
  user(userId: Int!): User!
  userMessages(userId: Int!): [Message]!
  users: [User]!
}

type Subscription {
  createMessage: Message
}

type User {
  email: String
  id: Int
  name: String
  password: String
}
