### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Edge {
  cursor: String
  node: Project
}

type Mutation {
  createProject(image: String!, name: String!): Project!
  createRepo(title: String!): Repo
  deleteProject(projectId: Int!): Project
}

type PageInfo {
  endCursor: String
  hasNextPage: Boolean
}

type Project {
  id: Int
  image: String
  name: String
}

type Query {
  project(projectId: Int!): Project!
  projects(after: Int, first: Int): Response
  repoSummary(limit: Int!, offset: Int!): RepoSummary
  repos(limit: Int!, offset: Int!): [Repo]
}

type Repo {
  id: Int
  title: String
}

type RepoSummary {
  count: Int
  repos: [Repo]
}

type Response {
  edges: [Edge]
  pageInfo: PageInfo
}
